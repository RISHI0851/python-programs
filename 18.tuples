# Sample tuple
numbers = (12, 45, 3, 67, 21, 89, 5)

# Find maximum and minimum using built-in functions
max_value = max(numbers)
min_value = min(numbers)

print("Tuple:", numbers)
print("Maximum value:", max_value)
print("Minimum value:", min_value)
